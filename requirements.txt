# Core ML / Deep Learning (Docker 이미지의 torch 2.3.1+cu121 유지)
# torch, torchvision, torchaudio는 Docker 이미지에 이미 설치되어 있으므로 생략
# vllm은 generate.py에서 사용하지 않으므로 제거 (설치 시 torch를 덮어쓰는 문제 방지)

# Hugging Face ecosystem (PyTorch 2.3.x 호환 버전 고정)
transformers>=4.36.0,<4.46.0
accelerate>=0.25.0,<1.0.0
datasets>=2.16.0,<3.0.0
evaluate>=0.4.0
sentence-transformers>=2.3.0,<3.0.0

# Metrics & Evaluation
torchmetrics>=1.2.0
rouge-score>=0.1.2
selfcheckgpt>=0.1.2
scikit-learn>=1.3.0

# Data processing
numpy>=1.24.0
pandas>=2.0.0
pandarallel>=1.6.5

# Utilities
tqdm>=4.66.0
matplotlib>=3.7.0
openai>=1.0.0
ipdb>=0.13.0
